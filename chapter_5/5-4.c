/* Write the function strend(s, t), which returns 1 if the string t occurs at
 * the end of the string, and zero otherwise. */

// while *s, while *t
// traverse through each character array and locate their null terminators
// traverse the character arrays again but in backwards this time
// if you end up traversing the entire t then return 1, otherwise return 0
